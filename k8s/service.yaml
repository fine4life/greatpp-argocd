apiVersion: v1
kind: Service
metadata:
  name: greatapp-argocd
spec:
  type: ClusterIP
  selector:
    app: greatapp-argocd
  ports:
    - name: http
      port: 80
      targetPort: 5170
      protocol: TCP
